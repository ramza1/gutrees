<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="google-site-verification" content="AvA6StoTzBsmTz2y59-i-Myrg1kwi-lFz5J8FDsL1AI" />
    <title><%= content_for?(:title) ? yield(:title) : "Gutrees" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>


  <%= render 'layouts/header' %>
   <% if content_for?(:publisher_content) %>
      <div class="secondary_nav">
        <%= render "top_panel", :branch => @branch_details %>
        <div class="container-fluid">
          <div class="row-fluid a_main user">
            <%= render "aside", :branch => @branch_details %>
            <section class=" center_panel">
              <div class="container-fluid <% if action_name == "sub_branches"%>extra-margin-top<% end %>">
                <%= render "about", :branch => @branch_details %>
                <%= render "flash_messages" %>
                <%= yield(:publisher_content) %>
              </div>
            </section>
      <aside class="right_panel">
         <%= render "right_panel" %>
      </aside>
      </div>
      </div>  </div>
   <% else %>
  <div class="secondary_nav">
    <%= render "top" %>
      <div class="container-fluid normal">
        <div class="row-fluid a_main ">
          <%= render "left_panel" %>
          <%= yield %>
          <aside class="right_panel">
            <%= render "right_panel" %>
          </aside>
        </div>
      </div>
  </div>
   <% end %>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
